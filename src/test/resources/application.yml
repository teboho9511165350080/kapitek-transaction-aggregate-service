spring:
  main:
    lazy-initialization: true

kapitek:
  customer:
    info:
      url: http://localhost:8091/kapitek-cif/info
      auth:
        client-id: kapitek-tx-aggregate-categorize-service
        client-secret: kapitek-tx-aggregate-categorize-service
        grant-type: client_credentials
        url: http://localhost:8080/realms/payments-gateway/protocol/openid-connect/token

  datasource:
    transaction:
      account:
        auth:
          client-id: kapitek-tx-aggregate-categorize-service
          client-secret: kapitek-tx-aggregate-categorize-service
          grant-type: client_credentials
          url: http://localhost:8080/realms/payments-gateway/protocol/openid-connect/token
        url: http://localhost:8091/kapitek-account/transactions
      card:
        auth:
          client-id: kapitek-tx-aggregate-categorize-service
          client-secret: kapitek-tx-aggregate-categorize-service
          grant-type: client_credentials
          url: http://localhost:8080/realms/payments-gateway/protocol/openid-connect/token
        url: http://localhost:8091/kapitek-card/transactions


server:
  port: 9080